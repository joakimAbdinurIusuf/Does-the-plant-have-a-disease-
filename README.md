# Does the Plant Have a Disease? (fast.ai, lesson 1)
Built a binary image classifier distinguishing diseased from healthy plants by fine-tuning a pretrained ResNet18 with fastai. Adapted the lesson-1 workflow to a self-chosen problem — assembled the image dataset, set up the data pipeline, and fine-tuned the model for the new task.
